const prompt = require("prompt-sync")();
class BankAccount {
	#accountNumber = "123456";
	#balance = 1000;
}

let amount = prompt("How much money do you want to deposit or withdraw?");
function deposit(amount) {
	constructor(amount) {
        this.balance = this.balance - amount
    } 
    let money = new BankAccount(``)
	console.log();
}
deposit(amount);
